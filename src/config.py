# Define some configurable variables

# Configurable path for storing specifications of tools
specifications_path = './config'

# URL for storing and retrieve memory content in the database
db_url = "redis://127.0.0.1:6379/0"

# Configurable path for storing permission information
permanent_permissions_path = "./config/permissions.json"

# Configurable user id, take "0" as an example
user_id = "0"

# Configurable path for storing functionality information
functionalities_path = './config/functionalities.json'